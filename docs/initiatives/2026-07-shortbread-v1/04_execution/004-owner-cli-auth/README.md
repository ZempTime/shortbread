# Ticket #4 — Owner and remote CLI authentication

This issue-scoped workspace owns GitHub #4 / T03: single-use Owner bootstrap, passkey re-entry/recovery, browser-assisted CLI authorization, named profiles/keyring storage, scoped automation tokens, and a stable authenticated `/api/v1` boundary.

Start with [`RUN.md`](RUN.md). Evidence and fixed-SHA review records belong under `evidence/` as the slice advances.
