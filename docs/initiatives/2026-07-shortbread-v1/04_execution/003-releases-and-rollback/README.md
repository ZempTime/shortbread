# Ticket #3 — Releases and rollback

This issue-scoped workspace owns GitHub #3 / T02: deterministic Manifest deltas, immutable Release history, atomic current-pointer advancement, and safe rollback across Rails, Owner UI, and the real CLI.

Start with [`RUN.md`](RUN.md). Evidence and fixed-SHA review records belong under `evidence/` as the slice advances.
