#!/usr/bin/env bash
set -euo pipefail

bin/rubocop
aube check

unformatted=$(cd cli && gofmt -l .)
if [[ -n "$unformatted" ]]; then
  echo "Go files require gofmt:" >&2
  echo "$unformatted" >&2
  exit 1
fi

(cd cli && go vet ./...)
